# in-toto layout — supply-chain step attestations for FactoryMind
expires: "2027-01-01T00:00:00Z"
keys: { project-key: { keyid: factorymind-key, keytype: ed25519, scheme: ed25519 } }
steps:
  - { name: source, expected_command: ["git", "clone"], pubkeys: [factorymind-key] }
  - { name: build, expected_command: ["docker", "build"], pubkeys: [factorymind-key] }
  - { name: test, expected_command: ["make", "test"], pubkeys: [factorymind-key] }
inspect:
  - { name: signed-image, run: ["cosign", "verify"], expected_materials: [], expected_products: [] }
