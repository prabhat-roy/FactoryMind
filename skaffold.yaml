# Skaffold pipeline for FactoryMind
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: factorymind }
build:
  artifacts:
    - { image: factorymind/api-gateway, context: src/api-gateway }
    - { image: factorymind/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: factorymind }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
