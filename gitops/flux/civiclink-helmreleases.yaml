# Flux uses HelmReleases generated by an external tool; one HelmRelease per service.
# This file references the index HelmRelease that points at the directory.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata: { name: factorymind-platform, namespace: factorymind }
spec:
  interval: 5m
  chart:
    spec:
      chart: helm/charts/api-gateway
      sourceRef: { kind: GitRepository, name: factorymind, namespace: flux-system }
  valuesFrom:
    - kind: ConfigMap
      name: factorymind-cloud-overlay
      valuesKey: cloud-values.yaml