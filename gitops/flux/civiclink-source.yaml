apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: factorymind, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/FactoryMind.git
  ref: { branch: main }