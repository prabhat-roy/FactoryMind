# OPAL — Open Policy Administration Layer for OPA
image: { repository: permitio/opal-server, tag: latest }
opal:
  policySource: { type: git, url: "https://github.com/factorymind/policies", branch: main }
  dataSources: [{ url: "http://config-service.factorymind.svc/policy-data", topics: ["policy_data"] }]
clients: { autoStart: true }
