opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: factorymind-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.factorymind.internal, paths: [{ path: /, pathType: Prefix }] }]
