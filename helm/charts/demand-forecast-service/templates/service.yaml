apiVersion: v1
kind: Service
metadata:
  name: {{ include "demand-forecast-service.fullname" . }}
  labels: {{- include "demand-forecast-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demand-forecast-service.selectorLabels" . | nindent 4 }}
