apiVersion: v1
kind: Service
metadata:
  name: {{ include "picking-service.fullname" . }}
  labels: {{- include "picking-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "picking-service.selectorLabels" . | nindent 4 }}
