cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: factorymind-physics-engine@{{ .Values.gcp.project }}.iam.gserviceaccount.com
