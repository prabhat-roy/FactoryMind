apiVersion: v1
kind: Service
metadata:
  name: {{ include "physics-engine.fullname" . }}
  labels: {{- include "physics-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "physics-engine.selectorLabels" . | nindent 4 }}
