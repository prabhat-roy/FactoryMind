apiVersion: v1
kind: Service
metadata:
  name: {{ include "rul-predictor.fullname" . }}
  labels: {{- include "rul-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rul-predictor.selectorLabels" . | nindent 4 }}
