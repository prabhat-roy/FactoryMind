apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmms-service.fullname" . }}
  labels: {{- include "cmms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cmms-service.selectorLabels" . | nindent 4 }}
