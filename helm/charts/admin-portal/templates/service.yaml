apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-portal.fullname" . }}
  labels: {{- include "admin-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "admin-portal.selectorLabels" . | nindent 4 }}
