apiVersion: v1
kind: Service
metadata:
  name: {{ include "ncr-service.fullname" . }}
  labels: {{- include "ncr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ncr-service.selectorLabels" . | nindent 4 }}
