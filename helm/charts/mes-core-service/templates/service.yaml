apiVersion: v1
kind: Service
metadata:
  name: {{ include "mes-core-service.fullname" . }}
  labels: {{- include "mes-core-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mes-core-service.selectorLabels" . | nindent 4 }}
