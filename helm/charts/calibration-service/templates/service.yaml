apiVersion: v1
kind: Service
metadata:
  name: {{ include "calibration-service.fullname" . }}
  labels: {{- include "calibration-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "calibration-service.selectorLabels" . | nindent 4 }}
