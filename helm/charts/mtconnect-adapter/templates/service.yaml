apiVersion: v1
kind: Service
metadata:
  name: {{ include "mtconnect-adapter.fullname" . }}
  labels: {{- include "mtconnect-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mtconnect-adapter.selectorLabels" . | nindent 4 }}
