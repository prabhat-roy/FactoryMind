apiVersion: v1
kind: Service
metadata:
  name: {{ include "quality-bff.fullname" . }}
  labels: {{- include "quality-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "quality-bff.selectorLabels" . | nindent 4 }}
