apiVersion: v1
kind: Service
metadata:
  name: {{ include "vibration-monitoring.fullname" . }}
  labels: {{- include "vibration-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "vibration-monitoring.selectorLabels" . | nindent 4 }}
