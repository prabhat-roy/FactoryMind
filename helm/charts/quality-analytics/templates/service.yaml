apiVersion: v1
kind: Service
metadata:
  name: {{ include "quality-analytics.fullname" . }}
  labels: {{- include "quality-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "quality-analytics.selectorLabels" . | nindent 4 }}
