apiVersion: v1
kind: Service
metadata:
  name: {{ include "training-compliance.fullname" . }}
  labels: {{- include "training-compliance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "training-compliance.selectorLabels" . | nindent 4 }}
