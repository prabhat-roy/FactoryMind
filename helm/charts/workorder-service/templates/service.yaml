apiVersion: v1
kind: Service
metadata:
  name: {{ include "workorder-service.fullname" . }}
  labels: {{- include "workorder-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "workorder-service.selectorLabels" . | nindent 4 }}
