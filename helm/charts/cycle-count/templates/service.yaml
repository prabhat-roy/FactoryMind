apiVersion: v1
kind: Service
metadata:
  name: {{ include "cycle-count.fullname" . }}
  labels: {{- include "cycle-count.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cycle-count.selectorLabels" . | nindent 4 }}
