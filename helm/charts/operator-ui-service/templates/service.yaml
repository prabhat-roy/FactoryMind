apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-ui-service.fullname" . }}
  labels: {{- include "operator-ui-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "operator-ui-service.selectorLabels" . | nindent 4 }}
