apiVersion: v1
kind: Service
metadata:
  name: {{ include "recall-service.fullname" . }}
  labels: {{- include "recall-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "recall-service.selectorLabels" . | nindent 4 }}
