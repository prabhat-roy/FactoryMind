apiVersion: v1
kind: Service
metadata:
  name: {{ include "kanban-service.fullname" . }}
  labels: {{- include "kanban-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "kanban-service.selectorLabels" . | nindent 4 }}
