apiVersion: v1
kind: Service
metadata:
  name: {{ include "cad-integration.fullname" . }}
  labels: {{- include "cad-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cad-integration.selectorLabels" . | nindent 4 }}
