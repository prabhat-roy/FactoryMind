apiVersion: v1
kind: Service
metadata:
  name: {{ include "scheduling-service.fullname" . }}
  labels: {{- include "scheduling-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "scheduling-service.selectorLabels" . | nindent 4 }}
