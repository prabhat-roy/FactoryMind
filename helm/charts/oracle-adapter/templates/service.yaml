apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-adapter.fullname" . }}
  labels: {{- include "oracle-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "oracle-adapter.selectorLabels" . | nindent 4 }}
