apiVersion: v1
kind: Service
metadata:
  name: {{ include "sap-adapter.fullname" . }}
  labels: {{- include "sap-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "sap-adapter.selectorLabels" . | nindent 4 }}
