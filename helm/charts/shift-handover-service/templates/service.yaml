apiVersion: v1
kind: Service
metadata:
  name: {{ include "shift-handover-service.fullname" . }}
  labels: {{- include "shift-handover-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "shift-handover-service.selectorLabels" . | nindent 4 }}
