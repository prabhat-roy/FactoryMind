apiVersion: v1
kind: Service
metadata:
  name: {{ include "master-schedule-service.fullname" . }}
  labels: {{- include "master-schedule-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "master-schedule-service.selectorLabels" . | nindent 4 }}
