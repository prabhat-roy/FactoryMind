apiVersion: v1
kind: Service
metadata:
  name: {{ include "revision-control.fullname" . }}
  labels: {{- include "revision-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "revision-control.selectorLabels" . | nindent 4 }}
