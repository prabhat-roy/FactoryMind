cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: factorymind-revision-control@{{ .Values.gcp.project }}.iam.gserviceaccount.com
