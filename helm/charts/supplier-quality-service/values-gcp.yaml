cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: factorymind-supplier-quality-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com
