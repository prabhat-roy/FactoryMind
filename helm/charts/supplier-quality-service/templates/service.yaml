apiVersion: v1
kind: Service
metadata:
  name: {{ include "supplier-quality-service.fullname" . }}
  labels: {{- include "supplier-quality-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "supplier-quality-service.selectorLabels" . | nindent 4 }}
