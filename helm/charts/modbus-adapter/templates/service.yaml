apiVersion: v1
kind: Service
metadata:
  name: {{ include "modbus-adapter.fullname" . }}
  labels: {{- include "modbus-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "modbus-adapter.selectorLabels" . | nindent 4 }}
