apiVersion: v1
kind: Service
metadata:
  name: {{ include "bom-service.fullname" . }}
  labels: {{- include "bom-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bom-service.selectorLabels" . | nindent 4 }}
