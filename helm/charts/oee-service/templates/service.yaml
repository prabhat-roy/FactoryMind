apiVersion: v1
kind: Service
metadata:
  name: {{ include "oee-service.fullname" . }}
  labels: {{- include "oee-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "oee-service.selectorLabels" . | nindent 4 }}
