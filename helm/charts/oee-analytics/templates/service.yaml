apiVersion: v1
kind: Service
metadata:
  name: {{ include "oee-analytics.fullname" . }}
  labels: {{- include "oee-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "oee-analytics.selectorLabels" . | nindent 4 }}
