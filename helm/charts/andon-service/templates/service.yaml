apiVersion: v1
kind: Service
metadata:
  name: {{ include "andon-service.fullname" . }}
  labels: {{- include "andon-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "andon-service.selectorLabels" . | nindent 4 }}
