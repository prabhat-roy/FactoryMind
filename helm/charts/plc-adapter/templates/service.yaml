apiVersion: v1
kind: Service
metadata:
  name: {{ include "plc-adapter.fullname" . }}
  labels: {{- include "plc-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "plc-adapter.selectorLabels" . | nindent 4 }}
