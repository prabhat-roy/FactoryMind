apiVersion: v1
kind: Service
metadata:
  name: {{ include "spc-service.fullname" . }}
  labels: {{- include "spc-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "spc-service.selectorLabels" . | nindent 4 }}
