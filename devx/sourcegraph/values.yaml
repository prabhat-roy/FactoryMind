# Sourcegraph (OSS) — universal code search across FactoryMind
image: { repository: sourcegraph/server, tag: 5.5.0 }
sourcegraph:
  externalURL: https://sourcegraph.factorymind.internal
  repositoriesToAdd: [github.com/prabhat-roy/FactoryMind]
ingress: { enabled: true, hosts: [{ host: sourcegraph.factorymind.internal }] }
