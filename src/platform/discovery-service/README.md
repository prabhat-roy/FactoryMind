# discovery-service

Lightweight in-mesh service discovery sidecar registry for legacy non-K8s integrations.
On Kubernetes the service mesh handles discovery directly; this exists for ERP and SCADA-side
adapters that run outside the cluster.
