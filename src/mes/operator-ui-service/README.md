# operator-ui-service

Server-side rendering host for the shop-floor operator HMI screens. Runs on the OT side,
backed by tracking + dispatch services. Designed to function with intermittent WAN.
Deployment overlay: `helm/charts/operator-ui-service/values-onprem.yaml`.
