// MTConnect adapter - pulls XML probe/current/sample documents and republishes via MQTT.
#include <iostream>

int main() {
    std::cout << "mtconnect-adapter starting" << std::endl;
    return 0;
}
