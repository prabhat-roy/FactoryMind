apiVersion: v1
kind: Service
metadata:
  name: rul-predictor
  namespace: factorymind
  labels: { app.kubernetes.io/name: rul-predictor, factorymind.io/domain: maintenance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: rul-predictor }
