apiVersion: v1
kind: Service
metadata:
  name: predictive-maintenance
  namespace: factorymind
  labels: { app.kubernetes.io/name: predictive-maintenance, factorymind.io/domain: maintenance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: predictive-maintenance }
