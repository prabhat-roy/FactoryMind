apiVersion: apps/v1
kind: Deployment
metadata:
  name: cmms-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: cmms-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: maintenance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: cmms-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cmms-service, factorymind.io/domain: maintenance }
    spec:
      serviceAccountName: cmms-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: cmms-service
          image: harbor.factorymind.internal/factorymind/cmms-service:latest
          ports: [{ containerPort: 50102, name: http }]
          envFrom: [{ configMapRef: { name: cmms-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
