apiVersion: v1
kind: Service
metadata:
  name: cmms-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: cmms-service, factorymind.io/domain: maintenance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: cmms-service }
