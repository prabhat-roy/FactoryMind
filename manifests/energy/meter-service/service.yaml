apiVersion: v1
kind: Service
metadata:
  name: meter-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: meter-service, factorymind.io/domain: energy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: meter-service }
