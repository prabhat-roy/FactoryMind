apiVersion: apps/v1
kind: Deployment
metadata:
  name: meter-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: meter-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: energy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: meter-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: meter-service, factorymind.io/domain: energy }
    spec:
      serviceAccountName: meter-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: meter-service
          image: harbor.factorymind.internal/factorymind/meter-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: meter-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
