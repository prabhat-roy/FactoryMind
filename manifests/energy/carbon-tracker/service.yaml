apiVersion: v1
kind: Service
metadata:
  name: carbon-tracker
  namespace: factorymind
  labels: { app.kubernetes.io/name: carbon-tracker, factorymind.io/domain: energy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: carbon-tracker }
