apiVersion: v1
kind: Service
metadata:
  name: oee-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: oee-service, factorymind.io/domain: energy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: oee-service }
