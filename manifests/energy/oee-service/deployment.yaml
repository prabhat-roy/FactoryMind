apiVersion: apps/v1
kind: Deployment
metadata:
  name: oee-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: oee-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: energy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: oee-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: oee-service, factorymind.io/domain: energy }
    spec:
      serviceAccountName: oee-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: oee-service
          image: harbor.factorymind.internal/factorymind/oee-service:latest
          ports: [{ containerPort: 50201, name: http }]
          envFrom: [{ configMapRef: { name: oee-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
