apiVersion: v1
kind: Service
metadata:
  name: audit-trail-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: audit-trail-service, factorymind.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: audit-trail-service }
