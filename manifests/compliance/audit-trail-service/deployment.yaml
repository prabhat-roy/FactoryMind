apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-trail-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: audit-trail-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: audit-trail-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: audit-trail-service, factorymind.io/domain: compliance }
    spec:
      serviceAccountName: audit-trail-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: audit-trail-service
          image: harbor.factorymind.internal/factorymind/audit-trail-service:latest
          ports: [{ containerPort: 50242, name: http }]
          envFrom: [{ configMapRef: { name: audit-trail-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
