apiVersion: v1
kind: Service
metadata:
  name: capa-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: capa-service, factorymind.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: capa-service }
