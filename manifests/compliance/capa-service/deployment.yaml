apiVersion: apps/v1
kind: Deployment
metadata:
  name: capa-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: capa-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: capa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: capa-service, factorymind.io/domain: compliance }
    spec:
      serviceAccountName: capa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: capa-service
          image: harbor.factorymind.internal/factorymind/capa-service:latest
          ports: [{ containerPort: 50241, name: http }]
          envFrom: [{ configMapRef: { name: capa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
