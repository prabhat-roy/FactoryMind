apiVersion: v1
kind: Service
metadata:
  name: batch-record-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: batch-record-service, factorymind.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: batch-record-service }
