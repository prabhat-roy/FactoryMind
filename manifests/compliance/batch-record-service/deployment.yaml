apiVersion: apps/v1
kind: Deployment
metadata:
  name: batch-record-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: batch-record-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: batch-record-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: batch-record-service, factorymind.io/domain: compliance }
    spec:
      serviceAccountName: batch-record-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: batch-record-service
          image: harbor.factorymind.internal/factorymind/batch-record-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: batch-record-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
