apiVersion: v1
kind: Service
metadata:
  name: training-compliance
  namespace: factorymind
  labels: { app.kubernetes.io/name: training-compliance, factorymind.io/domain: workforce }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: training-compliance }
