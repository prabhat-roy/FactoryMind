apiVersion: apps/v1
kind: Deployment
metadata:
  name: labour-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: labour-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: workforce
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: labour-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: labour-service, factorymind.io/domain: workforce }
    spec:
      serviceAccountName: labour-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: labour-service
          image: harbor.factorymind.internal/factorymind/labour-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: labour-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
