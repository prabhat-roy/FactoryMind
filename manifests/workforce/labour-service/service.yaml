apiVersion: v1
kind: Service
metadata:
  name: labour-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: labour-service, factorymind.io/domain: workforce }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: labour-service }
