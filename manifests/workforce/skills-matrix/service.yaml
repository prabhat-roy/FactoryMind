apiVersion: v1
kind: Service
metadata:
  name: skills-matrix
  namespace: factorymind
  labels: { app.kubernetes.io/name: skills-matrix, factorymind.io/domain: workforce }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: skills-matrix }
