apiVersion: v1
kind: Service
metadata:
  name: e-signature-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: e-signature-service, factorymind.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50023, targetPort: 50023 }]
  selector: { app.kubernetes.io/name: e-signature-service }
