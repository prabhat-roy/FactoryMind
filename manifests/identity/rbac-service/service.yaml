apiVersion: v1
kind: Service
metadata:
  name: rbac-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: rbac-service, factorymind.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: rbac-service }
