apiVersion: v1
kind: Service
metadata:
  name: operator-sso
  namespace: factorymind
  labels: { app.kubernetes.io/name: operator-sso, factorymind.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50021, targetPort: 50021 }]
  selector: { app.kubernetes.io/name: operator-sso }
