apiVersion: v1
kind: Service
metadata:
  name: kanban-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: kanban-service, factorymind.io/domain: supply-chain }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: kanban-service }
