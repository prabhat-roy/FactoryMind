apiVersion: apps/v1
kind: Deployment
metadata:
  name: kanban-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: kanban-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: supply-chain
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: kanban-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kanban-service, factorymind.io/domain: supply-chain }
    spec:
      serviceAccountName: kanban-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: kanban-service
          image: harbor.factorymind.internal/factorymind/kanban-service:latest
          ports: [{ containerPort: 50162, name: http }]
          envFrom: [{ configMapRef: { name: kanban-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
