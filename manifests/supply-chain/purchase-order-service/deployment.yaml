apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchase-order-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: purchase-order-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: supply-chain
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: purchase-order-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: purchase-order-service, factorymind.io/domain: supply-chain }
    spec:
      serviceAccountName: purchase-order-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: purchase-order-service
          image: harbor.factorymind.internal/factorymind/purchase-order-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: purchase-order-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
