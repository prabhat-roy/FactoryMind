apiVersion: v1
kind: Service
metadata:
  name: purchase-order-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: purchase-order-service, factorymind.io/domain: supply-chain }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: purchase-order-service }
