apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: supplier-service, factorymind.io/domain: supply-chain }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: supplier-service }
