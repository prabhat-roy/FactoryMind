apiVersion: apps/v1
kind: Deployment
metadata:
  name: supplier-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: supplier-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: supply-chain
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: supplier-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: supplier-service, factorymind.io/domain: supply-chain }
    spec:
      serviceAccountName: supplier-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: supplier-service
          image: harbor.factorymind.internal/factorymind/supplier-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: supplier-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
