apiVersion: v1
kind: Service
metadata:
  name: oee-analytics
  namespace: factorymind
  labels: { app.kubernetes.io/name: oee-analytics, factorymind.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: oee-analytics }
