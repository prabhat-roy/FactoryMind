apiVersion: apps/v1
kind: Deployment
metadata:
  name: yield-analytics
  namespace: factorymind
  labels:
    app.kubernetes.io/name: yield-analytics
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: yield-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: yield-analytics, factorymind.io/domain: analytics }
    spec:
      serviceAccountName: yield-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: yield-analytics
          image: harbor.factorymind.internal/factorymind/yield-analytics:latest
          ports: [{ containerPort: 50281, name: http }]
          envFrom: [{ configMapRef: { name: yield-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
