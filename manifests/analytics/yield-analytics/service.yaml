apiVersion: v1
kind: Service
metadata:
  name: yield-analytics
  namespace: factorymind
  labels: { app.kubernetes.io/name: yield-analytics, factorymind.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: yield-analytics }
