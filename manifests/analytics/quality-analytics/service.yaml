apiVersion: v1
kind: Service
metadata:
  name: quality-analytics
  namespace: factorymind
  labels: { app.kubernetes.io/name: quality-analytics, factorymind.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: quality-analytics }
