apiVersion: apps/v1
kind: Deployment
metadata:
  name: oracle-adapter
  namespace: factorymind
  labels:
    app.kubernetes.io/name: oracle-adapter
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: erp-integration
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: oracle-adapter } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: oracle-adapter, factorymind.io/domain: erp-integration }
    spec:
      serviceAccountName: oracle-adapter
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: oracle-adapter
          image: harbor.factorymind.internal/factorymind/oracle-adapter:latest
          ports: [{ containerPort: 50261, name: http }]
          envFrom: [{ configMapRef: { name: oracle-adapter } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
