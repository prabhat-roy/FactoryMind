apiVersion: v1
kind: Service
metadata:
  name: oracle-adapter
  namespace: factorymind
  labels: { app.kubernetes.io/name: oracle-adapter, factorymind.io/domain: erp-integration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: oracle-adapter }
