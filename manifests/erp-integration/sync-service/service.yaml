apiVersion: v1
kind: Service
metadata:
  name: sync-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: sync-service, factorymind.io/domain: erp-integration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: sync-service }
