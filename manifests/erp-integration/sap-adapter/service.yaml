apiVersion: v1
kind: Service
metadata:
  name: sap-adapter
  namespace: factorymind
  labels: { app.kubernetes.io/name: sap-adapter, factorymind.io/domain: erp-integration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: sap-adapter }
