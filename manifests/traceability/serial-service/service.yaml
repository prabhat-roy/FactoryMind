apiVersion: v1
kind: Service
metadata:
  name: serial-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: serial-service, factorymind.io/domain: traceability }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: serial-service }
