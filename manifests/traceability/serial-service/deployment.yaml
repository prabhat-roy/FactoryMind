apiVersion: apps/v1
kind: Deployment
metadata:
  name: serial-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: serial-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: traceability
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: serial-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: serial-service, factorymind.io/domain: traceability }
    spec:
      serviceAccountName: serial-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: serial-service
          image: harbor.factorymind.internal/factorymind/serial-service:latest
          ports: [{ containerPort: 50301, name: http }]
          envFrom: [{ configMapRef: { name: serial-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
