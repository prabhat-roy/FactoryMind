apiVersion: v1
kind: Service
metadata:
  name: lot-tracking
  namespace: factorymind
  labels: { app.kubernetes.io/name: lot-tracking, factorymind.io/domain: traceability }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: lot-tracking }
