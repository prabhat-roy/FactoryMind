apiVersion: v1
kind: Service
metadata:
  name: recall-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: recall-service, factorymind.io/domain: traceability }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: recall-service }
