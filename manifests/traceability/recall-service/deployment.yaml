apiVersion: apps/v1
kind: Deployment
metadata:
  name: recall-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: recall-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: traceability
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: recall-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: recall-service, factorymind.io/domain: traceability }
    spec:
      serviceAccountName: recall-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: recall-service
          image: harbor.factorymind.internal/factorymind/recall-service:latest
          ports: [{ containerPort: 50302, name: http }]
          envFrom: [{ configMapRef: { name: recall-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
