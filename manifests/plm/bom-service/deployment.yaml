apiVersion: apps/v1
kind: Deployment
metadata:
  name: bom-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: bom-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: plm
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: bom-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: bom-service, factorymind.io/domain: plm }
    spec:
      serviceAccountName: bom-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: bom-service
          image: harbor.factorymind.internal/factorymind/bom-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: bom-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
