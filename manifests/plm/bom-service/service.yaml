apiVersion: v1
kind: Service
metadata:
  name: bom-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: bom-service, factorymind.io/domain: plm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: bom-service }
