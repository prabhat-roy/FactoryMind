apiVersion: v1
kind: Service
metadata:
  name: cad-integration
  namespace: factorymind
  labels: { app.kubernetes.io/name: cad-integration, factorymind.io/domain: plm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: cad-integration }
