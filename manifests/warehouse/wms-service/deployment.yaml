apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: wms-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: warehouse
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: wms-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: wms-service, factorymind.io/domain: warehouse }
    spec:
      serviceAccountName: wms-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: wms-service
          image: harbor.factorymind.internal/factorymind/wms-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: wms-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
