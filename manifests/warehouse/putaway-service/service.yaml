apiVersion: v1
kind: Service
metadata:
  name: putaway-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: putaway-service, factorymind.io/domain: warehouse }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: putaway-service }
