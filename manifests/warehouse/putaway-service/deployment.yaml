apiVersion: apps/v1
kind: Deployment
metadata:
  name: putaway-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: putaway-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: warehouse
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: putaway-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: putaway-service, factorymind.io/domain: warehouse }
    spec:
      serviceAccountName: putaway-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: putaway-service
          image: harbor.factorymind.internal/factorymind/putaway-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: putaway-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
