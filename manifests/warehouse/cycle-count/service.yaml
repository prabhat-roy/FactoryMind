apiVersion: v1
kind: Service
metadata:
  name: cycle-count
  namespace: factorymind
  labels: { app.kubernetes.io/name: cycle-count, factorymind.io/domain: warehouse }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: cycle-count }
