apiVersion: v1
kind: Service
metadata:
  name: picking-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: picking-service, factorymind.io/domain: warehouse }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: picking-service }
