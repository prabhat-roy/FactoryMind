apiVersion: v1
kind: Service
metadata:
  name: dead-letter-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: dead-letter-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51010, targetPort: 51010 }]
  selector: { app.kubernetes.io/name: dead-letter-service }
