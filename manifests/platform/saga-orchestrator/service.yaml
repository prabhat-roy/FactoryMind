apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator
  namespace: factorymind
  labels: { app.kubernetes.io/name: saga-orchestrator, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51001, targetPort: 51001 }]
  selector: { app.kubernetes.io/name: saga-orchestrator }
