apiVersion: v1
kind: Service
metadata:
  name: idempotency-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: idempotency-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51000, targetPort: 51000 }]
  selector: { app.kubernetes.io/name: idempotency-service }
