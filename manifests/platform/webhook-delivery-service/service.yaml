apiVersion: v1
kind: Service
metadata:
  name: webhook-delivery-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: webhook-delivery-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51004, targetPort: 51004 }]
  selector: { app.kubernetes.io/name: webhook-delivery-service }
