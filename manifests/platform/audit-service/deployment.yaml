apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: audit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: audit-service, factorymind.io/domain: platform }
    spec:
      serviceAccountName: audit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: audit-service
          image: harbor.factorymind.internal/factorymind/audit-service:latest
          ports: [{ containerPort: 50002, name: http }]
          envFrom: [{ configMapRef: { name: audit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50002 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50002 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
