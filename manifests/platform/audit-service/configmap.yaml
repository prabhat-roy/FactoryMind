apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: audit-service, factorymind.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "50002"
