apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: audit-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50002, targetPort: 50002 }]
  selector: { app.kubernetes.io/name: audit-service }
