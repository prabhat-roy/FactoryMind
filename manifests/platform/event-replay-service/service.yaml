apiVersion: v1
kind: Service
metadata:
  name: event-replay-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: event-replay-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51009, targetPort: 51009 }]
  selector: { app.kubernetes.io/name: event-replay-service }
