apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: api-key-service, factorymind.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
