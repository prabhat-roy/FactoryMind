apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: scheduler-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50004, targetPort: 50004 }]
  selector: { app.kubernetes.io/name: scheduler-service }
