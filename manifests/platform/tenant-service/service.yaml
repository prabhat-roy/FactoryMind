apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: tenant-service, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51006, targetPort: 51006 }]
  selector: { app.kubernetes.io/name: tenant-service }
