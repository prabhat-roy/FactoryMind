apiVersion: v1
kind: Service
metadata:
  name: notification-orchestrator
  namespace: factorymind
  labels: { app.kubernetes.io/name: notification-orchestrator, factorymind.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50003, targetPort: 50003 }]
  selector: { app.kubernetes.io/name: notification-orchestrator }
