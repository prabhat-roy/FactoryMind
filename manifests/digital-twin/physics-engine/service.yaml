apiVersion: v1
kind: Service
metadata:
  name: physics-engine
  namespace: factorymind
  labels: { app.kubernetes.io/name: physics-engine, factorymind.io/domain: digital-twin }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: physics-engine }
