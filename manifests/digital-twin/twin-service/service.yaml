apiVersion: v1
kind: Service
metadata:
  name: twin-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: twin-service, factorymind.io/domain: digital-twin }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: twin-service }
