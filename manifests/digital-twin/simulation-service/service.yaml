apiVersion: v1
kind: Service
metadata:
  name: simulation-service
  namespace: factorymind
  labels: { app.kubernetes.io/name: simulation-service, factorymind.io/domain: digital-twin }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: simulation-service }
