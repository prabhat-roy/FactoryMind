apiVersion: apps/v1
kind: Deployment
metadata:
  name: simulation-service
  namespace: factorymind
  labels:
    app.kubernetes.io/name: simulation-service
    app.kubernetes.io/part-of: factorymind
    factorymind.io/domain: digital-twin
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: simulation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: simulation-service, factorymind.io/domain: digital-twin }
    spec:
      serviceAccountName: simulation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: simulation-service
          image: harbor.factorymind.internal/factorymind/simulation-service:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: simulation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
