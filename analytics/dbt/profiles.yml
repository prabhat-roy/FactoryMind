factorymind:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres.factorymind.svc
      user: dbt
      password: "{{ env_var('DBT_PASSWORD') }}"
      port: 5432
      dbname: factorymind_warehouse
      schema: public
      threads: 8
    prod:
      type: trino
      host: trino.factorymind.svc
      port: 8080
      catalog: iceberg
      schema: factorymind_mart
      threads: 16
