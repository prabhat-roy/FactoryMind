apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata: { name: factorymind-cpu-stress, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [factorymind] }
  stressors: { cpu: { workers: 4, load: 80 } }
  duration: 5m
