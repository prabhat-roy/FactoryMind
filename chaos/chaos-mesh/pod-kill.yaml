apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: { name: factorymind-pod-kill, namespace: chaos-mesh }
spec:
  action: pod-kill
  mode: one
  selector: { namespaces: [factorymind] }
  duration: 1m
  scheduler: { cron: "@every 1h" }
