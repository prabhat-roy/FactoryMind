apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: factorymind-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [factorymind] }
  timeOffset: "+500ms"
  duration: 3m
