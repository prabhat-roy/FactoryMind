apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: factorymind-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.factorymind.svc.cluster.local"]
  selector: { namespaces: [factorymind] }
  duration: 2m
