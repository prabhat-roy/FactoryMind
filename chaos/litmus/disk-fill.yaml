apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata: { name: factorymind-disk-fill, namespace: litmus }
spec:
  appinfo: { appns: factorymind, applabel: "tier=data", appkind: statefulset }
  chaosServiceAccount: litmus-admin
  experiments:
    - { name: disk-fill, spec: { components: { env: [ { name: FILL_PERCENTAGE, value: "80" }, { name: TOTAL_CHAOS_DURATION, value: "120" } ] } } }
